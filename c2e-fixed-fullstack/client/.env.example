# Frontend environment (Vite)
# In production (Vercel), set this in the Vercel project's Environment Variables
# (it is read at build time). It must point at the DEPLOYED backend API.
VITE_API_URL=https://c2e-fixed-fullstack.vercel.app/api
