Place a self-hosted `hero.mp4` here if you'd like to serve the background video
locally instead of the CDN URL used by default in `src/layouts/MainLayout.jsx`.

To switch to the local file, update the `<source>` src in `MainLayout.jsx` to:

    /videos/hero.mp4
